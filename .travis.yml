language: node_js
dist: trusty
sudo: required
branches:
  only:
  - master
before_script:
  - npm install -g @angular/cli

install:
  - npm install

script:
- ng lint
- ng build --prod --base-href https://t-palmer.github.io/travis-demo/